echarts中的配置   
toolbox: {
         show: true,
         feature: {
            dataZoom: {
               yAxisIndex: 'none',
               icon: {
                  zoom: `image://${zoomOutIcon}`, // 自定义放大图标路径
                  back: `image://${zoomOutIcon}`  // 自定义还原图标路径
               }
            },
         }
      },
功能为方法和缩小，它有自己的图标，我想自定义一个dom按钮，然后通过按钮去调用这个功能
怎么实现？
