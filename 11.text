      watch: {
   parameterDataList: {
      handler(newValue, oldValue) {
         console.log('对象内部值改变', newValue)
         this.parameterDataList = newValue.filter(item => item.isTransient !== "Transient")
      },
      deep: true
   }
  },
  运行报错：You may have an infinite update loop in watcher with expression "parameterDataList"，什么情况？
