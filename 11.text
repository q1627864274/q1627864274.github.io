      let p = new Promise((resolve, reject) => {
         resolve(124)
      })
      async function mm () {
         let s = 1233
         let n = await p
         return s
      }
      console.log(mm())
      // 打印的结果为什么是一个promise，而不是1233
