     // electron + vue3 +vite 项目中 使用node - serialport库进行串口通信

      // 发送都是通过write方法
      const port = new SeriaPort('COM6')
      port.write('数据')
      // 接收机器返回的数据，都是通过这个监听回来
      port.on('data', res => {
         console.log('每次接收的数据', data)
      });

      // 现在想弄成，promise形式，发送请求，然后再then方法中获取，例如在vue文件中进行类似与
      xxx().then((res) => {

      })
      // 怎么写？
