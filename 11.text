   let match = data.match(/-(\w+)-(\d+(-\d+)?\s?SCCM)-/);
   if (match) {
      return [match[1], match[2].replace(' SCCM', '')];
   }
   return [];
   对上述代码进行扩展，使它能处理'质量流量控制器-气体质量流量控制器-热式-N2-1-3 SLM-NC-IGS-Ethercat-24V'
   有SLM的
