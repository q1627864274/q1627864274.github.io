   if (data.code === 200) {
        if (data.data) {
          let times =
            new Date().getTime() - new Date(data.data.currentTime).getTime();
         console.log('times', times)

          this.timer = setInterval(() => {
            this.dynamicTime = new Date(new Date().getTime() + times);
          }, 1000);
data.data是后端返回的数据，data.data.currentTime为这个2023-11-13 19:53:21
上述代码的含义是什么？
