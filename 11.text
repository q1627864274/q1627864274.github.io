    const info = reactive({
        name: "why",
        age: 18,
        friend: {
          name: "kobe"
        }
      })
     // 3.监听reactive数据变化后, 获取普通对象
      watch(() => ({ ...info }), (newValue, oldValue) => {
        console.log(newValue, oldValue)
      }, {
        immediate: true,
        deep: true
      })

上述代码中() => ({ ...info }),是什么写法？
