 function processString(str) {
         return str.split('-')
            .map(num => Math.round(parseFloat(num) * 0.9))
            .join('-');
      }
console.log(processString('10-30')) 
上述代码输出9-27
但是console.log(processString('1-3'))  输出1-3
现在对代码进行扩展，使得数据先保留两位小数再乘以0.9 最后结果也保留
两位小数
