vue2开发中
handleActive() {
      this.status.forEach(item => {
         console.log('item', item)
         if (item.planStatus === 1 && item.activatedBy === item.createBy) {
            console.log(111)
            this.$message.warning(this.$t("dcpPlan.activeHint"));
            return;
         }
      })
}
// 针对上述函数return想要结束handleActive而不是forEach怎么写？
